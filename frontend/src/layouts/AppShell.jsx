import{AnimatePresence,motion}from'motion/react';import{Sidebar}from'../components/navigation/Sidebar';import{Topbar}from'../components/navigation/Topbar'
export function AppShell({mobileNavOpen,setMobileNavOpen,children}){return <div className="app-shell"><Sidebar open={mobileNavOpen} onNavigate={()=>setMobileNavOpen(false)}/><AnimatePresence>{mobileNavOpen&&<motion.button className="overlay mobile-only" aria-label="Close navigation" onClick={()=>setMobileNavOpen(false)} initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}}/>}</AnimatePresence><div className="app-column"><Topbar onMenu={()=>setMobileNavOpen(true)}/><main className="main">{children}</main></div></div>}
